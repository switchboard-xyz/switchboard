import path from "path";
import fs from "fs";
import { execSync } from "child_process";

async function main() {}

main()
  .then()
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
