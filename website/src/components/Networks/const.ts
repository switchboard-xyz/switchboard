import * as networks from "@switchboard-xyz/common/networks";

export default {
  aptos: networks.aptos,
  coredao: networks.coredao,
  near: networks.near,
  solana: networks.solana,
  sui: networks.sui,
};
