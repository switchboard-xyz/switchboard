# Secrets Server

- Managed service to deliver secret values to your Switchboard Function
- Users can either spin up their own secrets server or use Switchboard's managed service
- Users specify the enclave measurements they will let read secrets
- Switchboard functions can request data from a secrets server by generating a quote and a key to encrypt secrets with
- If the quote measurement is allowed, the function will be sent back the encrypted secrets
