---
hide_table_of_contents: true
keywords:
  [Switchboard, web3, oracle, data feeds, vrf, randomness, solana, aptos, near]
---

import AddressesGroup from "/src/components/Addresses/AddressesGroup";

# The Network

The Switchboard program is deployed on multiple chains, each with a native
deployment with isolated oracle queues where the queue authority can configure
the settings to influence its security.

Switchboard currently supports a native deployment on the following chains and
networks:

<AddressesGroup />
