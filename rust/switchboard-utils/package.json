{
  "name": "switchboard-utils",
  "scripts": {
    "build:cargo": "cargo build -p switchboard-utils",
    "lint": "cargo fmt -p switchboard-utils",
    "test": "cargo test -p switchboard-utils"
  }
}
